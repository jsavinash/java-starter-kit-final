// ============================================================================
// Java Starter Kit - Monorepo Root Build Script
// ============================================================================
// Centralized configuration for all subprojects. This eliminates the need
// to repeat group/version declarations in every module's build.gradle.kts.
// ============================================================================

// Common configuration for all subprojects
subprojects {
    // Apply common group and version to all modules
    group = "com.javastarterkit"
    version = "1.0.0-SNAPSHOT"
}